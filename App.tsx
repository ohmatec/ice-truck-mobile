// Minimal App entry for expo-router
// This file exists so imports from './App' (like in index.js) resolve correctly.
// It re-exports the router entry which provides the root component for the app.
export { default } from 'expo-router/entry';
